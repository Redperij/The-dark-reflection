scripted_gui = { # тип скрипта с вики
    court_button = { #ДОЛЖНО СОВПАДАТЬ С ИМЕНЕМ КОНТЕЙНЕРА !!! (из гуи)
        context_type = player_context #Интерфейс доступен только игроку (ИИ нахер)
		parent_window_token = politics_tab #Привязка к существующему ванильному окну (окно флага)
		window_name = "court_button" #Окно стоит называть так же
		
		visible = { #Условия, при которых видно кнопку
			is_ai = no #Пендель ИИ, только игрок видит 
		}
		
		effects = { #Эффекты от окна, действуют по аналогии с ивентами
			Severiana_Court_click = { #АХТУНГ !!! МОМЕНТ ОБСЕРА !!! Тут ИМЯКНОПКИ_click и никак иначе!
				if = { #Условие срабатывания кнопки, классика 
					limit = { NOT = { has_variable = global.open_door } }   #Если (обозначает limit), у нас НЕ (NOT) существует переменной open_door, мы ее создаем
					set_variable = { global.open_door = 1 } #Создаем переменную set_variable = { global.open_door = 1 }
				} #Если переменная существует, удаляем ее 
				else = {
					clear_variable = global.open_door
				} #Таким образом регулируется видимость окна, классика но я потiк (синтаксис говно)
			}
		}
        
		triggers = { #Типо у самой кнопки могут быть триггеры и условия. К примеру, ее появление от числа нажатий и т. д.
        } 
    }
}